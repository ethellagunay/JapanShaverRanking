{
  "name": "japan-mens-electric-shaver-rankings",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Static summary site for Japan men's electric shaver rankings.",
  "scripts": {
    "fetch": "node scripts/fetch-rankings.ts",
    "detect": "node scripts/detect-changes.ts",
    "update": "node scripts/update-rankings.ts",
    "build": "node scripts/build-site.ts",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=24"
  }
}
